/*
    File:   436365662.c - A.B.C三个数，A比B多8，C比B少3，谁最大？最大的比最小的多多少？
    Copyright:  © 2020-2025 brave_brother. All rights reserved.
 */
extern int printf(const char *format, ...);

#define MAX(a, b) ((a) > (b) ? (a) : (b))

int main()
{
    int A, B, C;
    A = B + 8;
    C = B - 3;
}