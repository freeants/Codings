/*
 * SameBirth.c - Calculate the possibility of N 
 *               people where two share the same birthday.
 * Pn = 1 - factorial(365) / (365**n * factorial(365-n))
 */

#include <stdio.h>
#include <gmp.h>

int main(int argc, char const *argv[])
{
    mpz_fac fact =1;
    fact.

    return 0;
}
